[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: marks smoke tests for quick regression
    slow: marks tests as slow (deselect with '-m "not slow"')
    api: marks tests that require the API server
    integration: marks integration tests
